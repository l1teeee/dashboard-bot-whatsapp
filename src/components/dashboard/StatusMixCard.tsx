import { Cell, Pie, PieChart, ResponsiveContainer } from 'recharts'; import { STATUS_META } from '@/lib/orderStatus'; import type { OrderStatus } from '@/types/order';
const colors:Record<OrderStatus,string>={pending:'#FFD468',processing:'#7472FF',completed:'#C9D9D1',cancelled:'#FF685C'};
export function StatusMixCard({counts,rate}:{counts:Record<OrderStatus,number>;rate:number}){const data=(Object.keys(counts) as OrderStatus[]).map(status=>({name:STATUS_META[status].label,value:counts[status],status}));return <section className="rounded-[30px] border border-ink-dark bg-mint p-5 text-ink-dark neo-shadow"><p className="kicker">Estados del lote</p><div className="mt-1 flex items-end justify-between"><h2 className="font-display text-3xl font-bold uppercase">Mezcla actual</h2><span className="text-right text-[10px] font-bold uppercase">Éxito entre cerrados<br/>{Math.round(rate*100)}%</span></div><div className="mt-2 h-36"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={38} outerRadius={62} paddingAngle={3}>{data.map(item=><Cell key={item.status} fill={colors[item.status]}/>)}</Pie></PieChart></ResponsiveContainer></div><ul className="grid grid-cols-2 gap-2">{data.map(item=><li key={item.status} className="flex items-center gap-2 text-xs font-bold"><span className="h-2.5 w-2.5 rounded-full border border-ink-dark" style={{backgroundColor:colors[item.status]}}/>{item.name} <span className="ml-auto">{item.value}</span></li>)}</ul></section>;}
